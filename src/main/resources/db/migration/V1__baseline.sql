-- Flyway baseline marker. Supabase owns migrations <= V1 (see
-- ShipSmart-Infra/supabase/migrations). With baseline-on-migrate=true
-- and baseline-version=1, Flyway records the existing Supabase schema
-- as the baseline and does not execute this file.
SELECT 1;
